#include <bits/stdc++.h>
using namespace std;

string str;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> str;

    cout << ":fan::fan::fan:" << '\n';
    cout << ":fan::" << str << "::fan:" << '\n';
    cout << ":fan::fan::fan:";

    return 0;
}